[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.build]
cmds = ["cd backend && pip install --no-cache-dir -r requirements.txt"]

[phases.start]
cmds = ["cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
