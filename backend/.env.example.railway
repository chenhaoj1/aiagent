# ============================================
# Railway 环境变量配置
# ============================================
# Railway 会自动提供以下环境变量:
# - DATABASE_URL (PostgreSQL)
# - PORT (默认 8000)

# 应用配置
APP_NAME=AI智能体平台
DEBUG=false
API_PREFIX=/api/v1

# 安全密钥 (Railway 可设置自动生成)
SECRET_KEY=${RAILWAY_PRIVATE_DOMAIN}

# AI API Keys (手动设置)
QWEN_API_KEY=your-qwen-api-key
DASHSCOPE_API_KEY=your-dashscope-api-key

# Redis (Railway 可添加 Redis 插件)
REDIS_URL=${REDIS_URL}

# CORS 前端域名
CORS_ORIGINS=["https://your-project.vercel.app"]
